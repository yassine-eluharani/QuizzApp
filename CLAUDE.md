<!-- skills-disposition: catalog-version=2026-04-27-73f386cf3fe4 updated=2026-04-28 -->

## Skills disposition

**Active in this project:**

- `app-icon` (external from `Code-with-Beto/skills`) — generate Expo app icons via SnapAI/OpenAI; Android adaptive icon variant
- `app-store-screenshots` (external from `ParthJadhav/app-store-screenshots`) — Next.js generator for iOS/Android store screenshots and Play feature graphic
- `react-native-expert` (plugin-skill from `fullstack-dev-skills`) — RN/Expo is the entire frontend stack
- `typescript-pro` (plugin-skill from `fullstack-dev-skills`) — TypeScript used across frontend and backend
- `secure-code-guardian` (plugin-skill from `fullstack-dev-skills`) — active security-hardening work; `lib/security.ts`
- `security-reviewer` (plugin-skill from `fullstack-dev-skills`) — audit support for the security hardening branch
- `postgres-pro` (plugin-skill from `fullstack-dev-skills`) — backend uses `pg` directly with no ORM
- `devops-engineer` (plugin-skill from `fullstack-dev-skills`) — Docker/EAS Build for production launch
- `brainstorming` (plugin-skill from `superpowers`) — pre-implementation exploration
- `dispatching-parallel-agents` (plugin-skill from `superpowers`) — concurrent independent tasks
- `executing-plans` (plugin-skill from `superpowers`) — running written implementation plans
- `finishing-a-development-branch` (plugin-skill from `superpowers`) — branch integration decisions
- `receiving-code-review` (plugin-skill from `superpowers`) — handling review feedback
- `requesting-code-review` (plugin-skill from `superpowers`) — pre-merge verification
- `subagent-driven-development` (plugin-skill from `superpowers`) — in-session subagent execution
- `systematic-debugging` (plugin-skill from `superpowers`) — bug investigation methodology
- `test-driven-development` (plugin-skill from `superpowers`) — TDD for features and bugfixes
- `using-git-worktrees` (plugin-skill from `superpowers`) — isolated feature workspaces
- `using-superpowers` (plugin-skill from `superpowers`) — superpowers entry skill
- `verification-before-completion` (plugin-skill from `superpowers`) — evidence before "done"
- `writing-plans` (plugin-skill from `superpowers`) — multi-step task planning
- `writing-skills` (plugin-skill from `superpowers`) — skill authoring

**Declined:**

- `claude-md-improver` — `.ai/CLAUDE.md` is small and gitignored; not enough surface to justify
- `test-master` — Jest configured but minimal test coverage today; revisit if test work picks up

**Deferred:** _none_
